import "package:flutter/material.dart";

class ExplorerScene extends StatelessWidget{
  @override
    Widget build(BuildContext context) {
      return Scaffold(
        body: Center(
          child: Text('explorer'),
        ),
      );
    }
}